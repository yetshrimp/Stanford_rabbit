#ifndef CALLBACK_H
#define CALLBACK_H

#include "shader.h"
#include "camera.h"
#include "model.h"

using namespace std;

//Shader * shader;
//Camera * camera;
//Model * model;

//double deltaTime;                         // 当前帧与上一帧的时间差
//double lastFrame;                         // 上一帧的时间

//bool firstMouse;                          // 系统刚启动时设置鼠标的初始位置
//double lastX;                             // 光标上一次的x轴位置
//double lastY;                             // 光标上一次的y轴位置
//double xOffset;                           // 光标移动的x轴位置
//double yOffset;                           // 光标移动的y轴位置

#endif // CALLBACK_H
